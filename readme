An x client, disguised as a de or wm.

deps:
	x client library package for your package manager. The package is usually named something like 'libxcb' or 'libxcb-dev'. Other than that, this just uses the c standard library.